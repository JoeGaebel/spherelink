<%= link_to "Login", login_path, class: "user-login josefin demo" if !logged_in? %>

<div class="container">
  <div class="header user-header">
    <span class="title"><%= @memory.name %></span>
  </div>

  <div class="viewer">
    <%= render "shared/memory_application", hideNavBar: true, style: "width: 100%; height: 600px;" %>
  </div>

  <div class="josefin description show-description">
    <%= @memory.description %>
  </div>
</div>

<% if logged_in? %>
  <%= link_to "Back to memories...", memories_path, class: "memories-link josefin" %>
<% else %>
  <%= link_to "Create your own!", signup_path, class: "memories-link josefin" %>
<% end %>
