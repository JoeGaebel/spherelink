# See https://help.github.com/articles/ignoring-files for more about ignoring files.

# ── Webapp (Rails) ──
webapp/.bundle
webapp/log/*
webapp/tmp/*
!webapp/log/.keep
!webapp/tmp/.keep
webapp/.byebug_history
webapp/.idea
webapp/public/uploads
webapp/public/assets
webapp/node_modules
webapp/config/application.yml

# ── Desktop (Electron) ──
desktop/node_modules
desktop/dist

# ── Editor / OS ──
.idea
.DS_Store
